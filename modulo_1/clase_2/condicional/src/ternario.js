let nombre = prompt('¿nombre?', '');

let message = (nombre == 'Juan') ? '¡Hola, Juan!' :
  (nombre == 'Luis') ? '¡Felicidades Luis!' :
  '¡Qué nombre tan inusual!';

//
  (nombre == 'Juan') ? pedirNombre(nombre) :
  (nombre == 'Pedro') ? '¡Hola Pedro!' :
  (nombre == 'Luis') ? '¡Felicidades Luis!' :
  '¡Qué nombre tan inusual!';
